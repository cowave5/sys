server:
  port: 19050
  servlet:
    context-path: /resource

spring:
  application:
    name: sys-resource
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/sys-resource
    username: postgres
    password: postgres
  liquibase:
    enabled: true
    change-log: sql/changelog.yml
  access:
    always-success: true
